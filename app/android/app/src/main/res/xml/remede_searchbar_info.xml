<?xml version="1.0" encoding="utf-8"?>
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/app_widget_description"
    android:initialKeyguardLayout="@layout/remede_searchbar"
    android:initialLayout="@layout/remede_searchbar"
    android:minWidth="240dp"
    android:maxWidth="300dp"
    android:maxResizeWidth="300dp"
    android:maxResizeHeight="40dp"
    android:minHeight="40dp"
    android:previewImage="@drawable/searchbar"

    android:previewLayout="@layout/remede_searchbar"
    android:targetCellWidth="4"
    android:targetCellHeight="1"
    android:updatePeriodMillis="86400000"
    android:resizeMode="horizontal"
    android:widgetCategory="home_screen" />